package app_test

import (
	"testing"
)

func Test_NewUseCases(t *testing.T) {
	//ass := assert.New(t)

	//workers := new(app.Workers)
	//workers.Init(3, 5)

	/*job := app.Job{}
	job1 := app.Job{}
	job2 := app.Job{}
	job3 := app.Job{}

	workers.AddWork(job)
	workers.AddWork(job1)
	workers.AddWork(job2)
	workers.AddWork(job3)

	time.Sleep(2 * time.Second)*/

}
